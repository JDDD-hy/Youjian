# 专注健康检查规则

状态：已实现
确认日期：2026-08-11
Policy 版本：`1`

## 1. 目标与边界

友间在单次会话累计有效专注达到 2 小时时进行一次健康检查，邀请用户休息。用户可以结束，也可以明确选择继续；未在规定时间内选择时，由服务器自动结束当前会话。

该功能不是新的 2 小时硬上限。现有单次会话 6 小时硬上限继续保留。

本规则对所有用户一致生效，不针对特定用户开启，也不提供永久关闭开关。

## 2. 计时与触发

- 按累计有效专注时间计算，暂停时间不计入。
- 每个适用会话在累计有效专注达到 2 小时时最多检查一次。
- 检查开始后等待 1 分钟。
- 等待期间继续计入有效专注。
- 1 分钟内没有被服务器接受的有效选择时，服务器自动结束会话。
- 选择继续后，本会话不再进行健康检查，最多继续至现有 6 小时硬上限。
- 选择结束或自动结束后，可以立即开始新会话，不设置冷却时间。

服务器是截止时间、选择结果和最终结算的权威来源。客户端计时、点击时间或通知展示状态不能覆盖服务器结果。

## 3. 近期休息豁免

符合以下全部条件的暂停视为已经完成本次健康检查：

- 暂停持续至少 5 分钟；
- 暂停结束后，到累计有效专注达到 2 小时之间，新增有效专注不超过 30 分钟。

满足后记录 `health_check_satisfied_by_pause`，本会话不再询问，但仍受 6 小时硬上限约束。

如果健康检查已经开始后会话被暂停：

- 立即挂起当前 1 分钟截止时间；
- 暂停满 5 分钟时通过检查；
- 5 分钟内恢复时，重新获得完整 1 分钟；
- 暂停满现有 15 分钟上限时，仍按 `pause_timeout` 结束。

## 4. 结果与事件

### 4.1 最终结束原因

| 稳定值 | 中文 UI 标签 | 含义 |
| --- | --- | --- |
| `health_check_accepted` | 两小时专注后主动结束 | 用户选择“收起此刻” |
| `health_check_timeout` | 两小时专注后自动结束 | 1 分钟内没有有效选择 |
| `focus_limit` | 沿用现有标签 | 选择继续后最终达到 6 小时 |

### 4.2 过程事件

- `health_check_triggered`
- `health_check_continued`
- `health_check_satisfied_by_pause`

`health_check_continued` 和 `health_check_satisfied_by_pause` 不是结束原因，不得写入 `completion_reason`。

## 5. 统计、目标与成就

通过 `health_check_accepted` 或 `health_check_timeout` 结束的会话：

- 状态为 `completed`；
- 正常计入专注统计；
- 正常计入个人目标和共享目标；
- 正常参与成就判定；
- 使用服务器结算的实际有效专注时长。

专注历史列表展示最终结束原因。选择继续和暂停满足休息只在会话详情或内部分析中展示，不占用历史列表主标签。

结构化导出保留稳定英文值，中文 UI 使用独立标签。

## 6. 前台交互

用户停留在友间前台时，显示应用内自定义 Modal，不使用 `window.confirm()`。

Modal 不允许通过关闭按钮、Esc 或点击遮罩退出，只能选择：

- `收起此刻`
- `我想继续专注`

正式文案：

> **时间走过了两个小时**
> 这一段专注已经足够漫长。
> 先让目光离开屏幕，也让思绪有片刻留白。
> 1 分钟内未选择，本次专注将自动结束。

倒计时必须独立、醒目地从 60 递减到 0，不能隐藏在氛围文案中。

## 7. 系统通知与 Focus Reminder

### 7.1 能力边界

- 复用当前 Focus Reminder 的系统通知机制，不建设服务器 Web Push。
- PWA 仍在运行、最小化或页面处于后台时，尽力显示系统通知。
- PWA 完全关闭、设备休眠、进程被回收、系统勿扰或通知权限被拒绝时，不保证通知可见。
- 无论通知是否显示，服务器健康检查规则和 1 分钟自动结束始终生效。
- 用户关闭桌面提醒不等于关闭健康检查。

### 7.2 通知优先级与互斥

通知优先级：

1. 健康检查
2. 暂停超时预警
3. 普通 Focus Reminder

健康检查开始时：

- 关闭同一会话当前可见的普通 Focus Reminder；
- 使用同一会话级通知 `tag` 替换，而不是叠加通知；
- 停止该会话后续普通“专注仍在进行”提醒。

### 7.3 健康检查通知

标题：

> 友间 · 灯光走过了两个小时

正文：

> 你已为「任务名称」亮灯两小时。
> 先让目光离开屏幕，也让思绪有片刻留白。
> 1 分钟内未选择，本次专注将自动结束。

按钮：

- `收起此刻`
- `继续专注`

按钮一键直接提交服务器，不要求打开友间再次确认。

如果平台不支持通知按钮，点击通知正文时打开或聚焦友间并显示剩余倒计时。点击通知右上角关闭或系统“关闭”不算继续，服务器倒计时保持不变。

## 8. 台灯图标

健康检查等待状态使用 `lamp-dimmed.svg` 的处理版本，表达“灯光收窄但尚未熄灭”。

当前视觉源位于构建输出 `dist/lamp-dimmed.svg`。实现时必须：

- 将受版本控制的源资源放入 `public`，不得依赖 `dist` 或 `file:///`；
- 去除不透明白底；
- 裁切多余留白；
- 保持整体台灯造型；
- 增强通知小尺寸下的灯架轮廓与暖色光锥；
- 输出适合通知与高分辨率显示的尺寸。

状态使用：

- 健康检查等待：处理后的 `lamp-dimmed.svg`；
- 主动结束或自动结束：`lamp-paused-fixed.svg`；
- 选择继续：恢复 `lamp-focusing-fixed.svg`。

## 9. 操作反馈

用户选择结束且服务器确认成功后，原通知替换为约 5 秒的结果通知：

> **友间 · 灯已收起**
> 本次共专注 **服务器实际时长**。现在让目光和思绪休息一会儿吧。

用户选择继续且服务器确认成功后：

> **友间 · 灯仍亮着**
> 已继续本次专注，之后不再进行休息询问，最长可继续至六小时。

1 分钟超时后，原通知更新为：

> **友间 · 灯已自动收起**
> 两个小时已经走过，本次专注已自动结束。
> 本次共专注 **服务器实际时长**。

用户从后台返回友间时，仍显示一次结果弹窗：

> **这一段专注，先停在这里**
> 两个小时已经走过，专注已于 **服务器结束时间** 自动结束。
> 本次共专注 **服务器实际时长**，现在让自己休息一会儿吧。

结果弹窗按会话记录一次已读状态，避免刷新后反复出现。

## 10. 并发、超时与失败

- 多标签页和多设备采用服务器最先成功写入的有效选择。
- 任一客户端选择成功后，其他页面立即同步最终结果并关闭对应提示。
- 请求等待期间倒计时继续。
- 请求失败且仍未到截止时间时，恢复按钮并允许重试。
- 服务器已经自动结束时，迟到的“继续”不得恢复原会话。
- 健康检查的逻辑截止时间固定为触发后 1 分钟；现有每分钟维护任务可以稍后执行结算，但 `completed_at` 和有效专注时长必须截断在该逻辑截止时间，不得把维护延迟计入专注。
- 选择请求失败时不得显示成功反馈。
- 若后台通知操作失败，打开或聚焦友间，展示剩余倒计时或最终结果。
- 所有结算、事件和目标/成就副作用必须幂等。

## 11. Policy 告知与确认

所有新用户和现有用户都必须按稳定用户身份确认一次 policy v1。

用户点击开始专注时，如果尚未确认：

1. 暂不开始计时；
2. 现有主卡片原地切换为 policy 确认步骤；
3. 不新增首页卡片、边框或更新横幅；
4. 点击返回时保留任务名称和分类；
5. 确认后保存 policy v1 并开始刚才填写的会话。

正式文案：

> **让灯光也记得休息**
> 每次累计专注两小时后，友间会邀请你稍作休息。
> 你可以结束本次专注，也可以继续至六小时；若 1 分钟内没有选择，本次专注将自动结束。
> 最近 30 分钟有效专注内，若已暂停满 5 分钟，将视为已经休息，不再询问。
> 即使关闭桌面通知，这项健康检查规则仍会生效。

按钮：

- `知道了，点亮台灯`
- `返回`

设置页永久提供只读规则说明，但不得提供关闭开关。桌面提醒设置与强制健康检查必须分开，避免用户误解。

实质规则变化时提升 policy 版本并重新告知；纯文案调整不应无故要求重新确认。

## 12. 上线与紧急关闭

- 完成测试后全量上线，不按用户定向，也不随机灰度。
- 仅对功能启用后开始的新会话生效。
- 上线时已经在进行或暂停的会话继续使用旧 6 小时规则。
- 提供服务器控制的紧急关闭开关。

紧急关闭时：

- 不创建新的健康检查；
- 取消尚未结算的健康检查并允许会话继续；
- 所有会话仍保留原 6 小时硬上限；
- 不删除或改写历史事件、结束原因或已完成统计。

服务器必须兼容旧缓存 PWA，不能在旧客户端无法处理 policy 错误时直接阻断开始专注。

## 13. 最小必要分析数据

记录：

- `health_check_triggered`
- `health_check_accepted`
- `health_check_timeout`
- `health_check_continued`
- `health_check_satisfied_by_pause`
- 系统通知调用成功或失败
- 自动结束后 5 分钟内是否开始新会话
- 各事件的会话数、匿名用户数和比例

不记录：

- 通知文字副本；
- 用户正在使用的其他应用；
- 与健康检查无关的设备指纹；
- “用户确实看见了通知”这一无法可靠证明的事实。

`showNotification()` 成功只表示浏览器接受显示请求，不表示用户实际看见通知。

## 14. 验收重点

- 累计有效专注与暂停口径准确；
- 2 小时、1 分钟和 6 小时边界准确；
- 近期 5 分钟暂停与 30 分钟有效专注窗口准确；
- 主动结束、超时、继续、暂停豁免互斥且幂等；
- 多标签页、多设备和截止时间并发结果唯一；
- Windows Edge PWA 通知按钮、替换、关闭和降级行为经过实机验证；
- 前台 Modal、后台通知和服务器状态一致；
- 通知关闭、PWA 关闭、断网、休眠、旧缓存客户端均有覆盖；
- 历史、统计、目标、成就和结构化导出使用正确口径；
- 现有会话豁免、policy v1 确认和紧急关闭开关经过验证；
- `lamp-dimmed.svg` 在系统通知小尺寸下清晰可辨。

## 15. 非目标

- 不把单次硬上限从 6 小时改成 2 小时；
- 不建设服务器 Web Push；
- 不保证系统通知越过权限、勿扰模式或已关闭的 PWA 展示；
- 不开发原生置顶桌面弹窗；
- 不设置跨会话强制冷却；
- 不为特定用户单独启用；
- 不永久关闭健康检查。

## 16. 实现难度摘要

综合难度为中高。功能的主要难点不是通知文案或弹窗视觉，而是服务器权威状态、1 分钟截止、暂停豁免、多端并发、结算幂等、旧客户端兼容，以及统计和成就副作用的一致性。
