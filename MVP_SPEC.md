# 友间（Youjian）MVP 产品与技术规格

> 名称：友间（Friend Space）  
> Slogan：在友间，自有间。  
> 文档状态：MVP 需求已冻结  
> 核心定位：面向 2～3 位好友、最多十几人的轻量共享专注空间，不替代任务管理工具。

## 1. 产品背景

用户可能分别使用国内版、海外版或其他任务管理工具，无法共享同一个自习室。友间只承担跨平台的“专注数据中间层”：成员继续在各自的工具中管理任务，在友间填写本次要做的事情、记录专注时间，并看到好友当前的专注状态。

友间不接入 TickTick／滴答清单的内部接口，不读取网页 Cookie、私有登录令牌或非官方 API。

## 2. MVP 目标

### 2.1 核心价值

- 通过固定好友的实时在场感降低彼此监督成本。
- 将“我正在学习”转化为可见、可累计的专注时间。
- 用连续打卡和共同目标促进长期使用。

### 2.2 首版成功指标

创建友间后的 14 天内，至少两名成员分别在 7 个不同自然日完成有效打卡。

辅助指标：

- 每周有效专注人数；
- 每位成员每周的有效专注天数；
- 至少两名成员在同一天完成打卡的天数；
- 开始专注后形成有效记录的比例。

## 3. 产品模型

友间必须区分长期空间与单次专注：

- **友间（Space）**：长期存在，保存成员、邀请、共同目标、成就和统计。
- **专注记录（Focus Session）**：一次从开始到结束或自动结算的专注过程。

结束专注只关闭本次专注记录，不关闭友间。成员下次直接回到原友间开始新记录，不需要重复建房。

## 4. 成员与邀请

### 4.1 身份方式

- 用户通过邀请链接进入友间；
- 首次进入只填写昵称，不要求邮箱、密码或第三方账号；
- 底层使用 Supabase Anonymous Auth 为设备生成独立用户 ID；
- 同一友间内昵称必须唯一；
- 身份默认绑定当前浏览器／设备；
- MVP 不支持多设备同步或匿名身份恢复。

### 4.2 身份丢失规则

以下操作可能导致匿名身份无法恢复：

- 清除浏览器数据；
- 主动退出匿名身份；
- 更换手机或浏览器；
- 删除 PWA 数据。

身份丢失后，用户只能以新身份重新加入。房主可以停用旧成员身份，但不能删除或转移其历史记录。旧记录继续归属于原身份并保留原昵称快照。

产品必须在首次加入和设置页清楚提示这一限制。

### 4.3 房主与邀请链接

- 创建友间的人永久成为房主；
- 房主可设置友间名称、房间时区和人数上限；
- 同一邀请链接可重复使用，直至达到人数上限；
- 房主可以重置邀请链接，使旧链接立即失效；
- 房主可以停用成员；
- 停用成员不删除其历史记录；
- MVP 不实现转让房主。

默认建议人数上限为 12，数据库层保留可调整能力。

## 5. 专注流程

### 5.1 开始专注

开始前必须填写：

- 任务名称：必填，1～80 字；
- 分类标签：可选，例如学习、工作、阅读、运动。

开始后任务名称和分类锁定，不允许修改。不同成员可以同时专注于完全不同的任务。

### 5.2 状态机

```text
focusing ──pause──> paused ──resume──> focusing
    │                  │
    ├──────end─────────┤
    │                  │
    └──6h auto-end     └──15m auto-end
                       │
                       ▼
                    completed
```

状态定义：

- `focusing`：正在累计有效专注时间；
- `paused`：暂时停止，不累计专注时间；
- `completed`：已结算，不能恢复、修改或删除。

同一成员在同一时间只能存在一条未完成的专注记录。

### 5.3 暂停与恢复

- 暂停期间不累计专注时间；
- 暂停不足 15 分钟可以恢复原记录；
- 暂停达到 15 分钟后自动结算；
- 自动结算的结束点为暂停开始时刻，而非第 15 分钟；
- 自动结算后不能恢复旧记录，只能开始新记录。

示例：10:00 开始，10:40 暂停，10:55 触发自动结算，最终专注时间为 40 分钟。

### 5.4 六小时上限

- 单次记录累计的实际专注时间最多为 6 小时；
- 暂停时间不计入六小时；
- 累计实际专注达到 6 小时后自动结算。

### 5.5 有效记录

- 单次实际专注少于 5 分钟，不计入日、周、月统计和打卡；
- 此类记录仍保留，状态标记为 `discarded`；
- 有效和无效历史记录均不可修改、不可删除；
- 自动结算记录必须保存结算原因。

结算原因至少包括：

- `manual_end`：用户主动结束；
- `pause_timeout`：暂停达到 15 分钟；
- `focus_limit`：实际专注达到 6 小时；
- `member_disabled`：房主停用成员时结算其活动记录。

## 6. 实时房间状态

友间首页主要显示当前正在专注的成员，而不是完整在线列表。

每位正在专注的成员显示：

- 昵称；
- 任务名称；
- 分类标签（如有）；
- 当前累计的实际专注时长；
- 是否处于连接状态不可确认区间。

示例：

> 赵琳正在专注：体育法阅读 · 36 分钟

允许数秒级同步延迟，不要求严格秒级一致。实时更新使用 Supabase Realtime，显示时长由服务器时间戳计算，不能使用客户端自行提交的累计分钟数。

## 7. 后台、断网与连接不确定性

### 7.1 基本规则

- 关闭网页、手机锁屏、PWA 进入后台或网络中断时，专注计时继续；
- 暂停和结束需要成功提交到服务器后才生效；
- 恢复连接后由服务器时间重新校准显示；
- 连接不可确认期间仍计入专注时间；
- 用户不能手动修改这段时间。

### 7.2 命名原则

网页无法可靠区分真实断网、手机锁屏、浏览器后台冻结和 Realtime 临时断线，因此不得将其确定地记录为 `network offline`。

统一使用：

- 中文状态：**连接状态不可确认**；
- 建议字段：`unconfirmed_connection`；
- 好友端提示：`连接中断或应用在后台，最后同步于 14:32`；
- 历史提示：`本次包含约 18 分钟连接状态不可确认区间`。

只按分钟展示未确认时长，不提供虚假的秒级精度。

### 7.3 近似计算

客户端在前台时定期发送轻量心跳。服务器保存 `last_seen_at`，超过阈值后将连接状态显示为不可确认。重新连接后结束该区间。

该数据只能反映连接／执行状态，不能证明用户是否真正专注，也不能准确判定断线原因。

## 8. 统计与跨日拆分

### 8.1 统计口径

- 暂停时间不计入专注时长；
- 单次不足 5 分钟的记录不参与累计；
- 跨越午夜的记录按照实际时间拆分到两个自然日；
- 日、周、月数据从专注区间计算，不信任客户端汇总值；
- 周周期为周一至周日；
- 月周期为自然月。

### 8.2 时区

- 每名成员保存个人 IANA 时区，例如 `Asia/Shanghai`；
- 个人日、周、月统计按个人时区计算；
- 每个友间保存一个房间时区；
- 共同目标、共同成就和“所有人今天都打卡”按房间时区计算；
- 房间时区创建后锁定，MVP 不支持修改。

### 8.3 连续打卡

- 连续打卡按个人分别计算；
- 当天多条有效专注记录可以累计；
- 单次不足 5 分钟的记录不参与当天累计；
- 达到每日累计门槛后，当天视为完成打卡；
- 多位成员同日完成打卡，可以触发共同成就。

**每日累计 60 分钟才算完成个人打卡。**

该数值保存在友间配置中，不得在前端业务逻辑中硬编码。

## 9. 共同目标

### 9.1 目标类型

MVP 只支持三类：

1. **小组合计**：例如本周所有成员共同专注 20 小时；
2. **个人门槛**：例如本周每位成员至少专注 5 小时；
3. **共同出勤**：例如本周至少 5 天所有成员都完成打卡。

周期只支持每日、每周和每月。

### 9.2 提案流程

```text
提案：pending → accepted / rejected / expired
                    │
                    ▼
目标：          scheduled → active → completed / failed
```

规则：

- 任一成员都可以提出共同目标；
- 其他当前成员只能接受或拒绝，不能直接在线修改原提案；
- 如需修改，应拒绝原提案并创建一个新提案；
- 提案创建时冻结需要投票的活动成员名单；
- 名单中的所有成员一致接受后，目标才通过；
- 48 小时未获得全员同意则自动过期；
- 新加入成员不追加到已有提案；
- pending 期间成员被停用时，从必需投票名单中移除并重新判断；
- 目标从下一个完整周期开始生效，不追溯当前周期；
- 目标通过时冻结参与成员；生效后的成员变化不推翻目标；
- 成员投票和目标历史不可删除。

## 10. 成就

MVP 可以先预留成就数据结构，首版界面只实现少量确定性成就，例如：

- 两人同日完成打卡；
- 全员连续 3 天完成打卡；
- 首次达成共同目标；
- 小组累计专注达到指定里程碑。

成就必须由服务器根据事实数据触发，客户端不能直接授予。

## 11. 防作弊边界

友间只承诺防止数据层面的篡改，不监控用户现实行为。

系统需要保证：

- 客户端不能指定开始、暂停、恢复或结束的服务器时间；
- 客户端不能直接提交累计专注分钟数；
- 用户不能修改或删除历史记录；
- 用户不能同时运行多条专注记录；
- 用户只能操作自己的进行中记录；
- 状态变化保留不可变事件；
- 所有自动结算由数据库规则执行。

系统不承诺判断：

- 用户是否真的在学习；
- 用户是否在使用其他 App；
- 用户是否离开座位；
- 连接状态不可确认的确切原因。

## 12. 技术方案

### 12.1 推荐技术栈

- 前端：React + TypeScript + Vite；
- UI：轻量响应式网页；
- PWA：Web App Manifest + Service Worker；
- 身份：Supabase Anonymous Auth；
- 数据库：Supabase PostgreSQL；
- 权限：PostgreSQL Row Level Security（RLS）；
- 实时同步：Supabase Realtime；
- 定时结算：Supabase Cron／`pg_cron`；
- 部署：Vercel 或 Cloudflare Pages。

选择 Supabase 而非 Firebase 的主要原因是：共同目标审批、不可变事件、跨日拆分、关系权限和统计聚合更适合 PostgreSQL。

### 12.2 服务端权威原则

所有重要写操作通过 PostgreSQL 函数或服务端接口完成：

- `create_space`
- `join_space`
- `start_focus`
- `pause_focus`
- `resume_focus`
- `end_focus`
- `settle_expired_sessions`
- `propose_goal`
- `vote_goal`

函数使用数据库当前时间 `now()`，客户端时间只用于界面显示，不参与最终结算。

### 12.3 自动结算策略

- Cron 每分钟扫描应自动结算的记录；
- 任何读取进行中状态或发起新操作时，也先执行惰性结算判定；
- 即使 Cron 短暂延迟，最终结果仍由保存的时间戳计算，不增加额外专注时长。

## 13. 建议数据模型

### 13.1 核心表

#### `profiles`

- `id`：对应 Supabase Auth 用户 ID；
- `timezone`；
- `created_at`。

昵称属于具体友间的成员身份，保存在 `space_members`，不作为全局 profile 字段。

#### `spaces`

- `id`；
- `name`；
- `owner_id`；
- `timezone`；
- `member_limit`；
- `invite_token_hash`；
- `created_at`。

#### `space_members`

- `space_id`；
- `user_id`；
- `display_name_snapshot`；
- `role`：`owner` 或 `member`；
- `status`：`active` 或 `disabled`；
- `joined_at`；
- `disabled_at`。

同一友间内 `display_name_snapshot` 对活动成员唯一。

#### `focus_sessions`

- `id`；
- `space_id`；
- `user_id`；
- `member_id`；
- `task_name`；
- `category`；
- `status`：`focusing`、`paused`、`completed` 或 `discarded`；
- `started_at`；
- `active_segment_started_at`；
- `paused_at`；
- `accumulated_focus_seconds`；
- `completed_at`；
- `completion_reason`；
- `last_seen_at`；
- `unconfirmed_connection_seconds`；
- `created_at`。

#### `focus_segments`

- `id`；
- `session_id`；
- `started_at`；
- `ended_at`。

每次开始或恢复产生一个实际专注区间，暂停或结束时关闭。跨日统计以该表为事实来源，从而天然排除暂停时间。

#### `focus_events`

- `id`；
- `session_id`；
- `event_type`：`started`、`paused`、`resumed`、`completed`、`connection_unconfirmed`、`reconnected`；
- `occurred_at`：服务器时间；
- `metadata`；
- `created_at`。

该表只允许追加，不允许更新或删除。

#### `focus_connection_intervals`

- `id`；
- `session_id`；
- `started_at`；
- `ended_at`；
- `detected_from_last_seen_at`。

该表保存近似的连接状态不可确认区间，不参与专注有效性判断。

#### `goal_proposals`

- `id`；
- `space_id`；
- `proposer_id`；
- `goal_type`；
- `period_type`；
- `target_value`；
- `status`；
- `expires_at`；
- `effective_period_start`；
- `created_at`。

#### `goal_proposal_members`

- `proposal_id`；
- `member_id`；
- `vote`：空、`accepted` 或 `rejected`；
- `voted_at`。

该表保存提案创建时的必需投票成员快照。每名成员对同一提案只能产生一个最终投票记录。

#### `goals`

- `id`；
- `source_proposal_id`；
- `space_id`；
- `goal_type`；
- `period_type`；
- `target_value`；
- `starts_at`；
- `ends_at`；
- `status`：`scheduled`、`active`、`completed` 或 `failed`；
- `completed_at`。

#### `goal_participants`

- `goal_id`；
- `member_id`。

该表在目标通过时冻结参与成员，避免目标生效后的成员变化重写规则。

#### `achievements`

- `id`；
- `space_id`；
- `achievement_type`；
- `earned_at`；
- `metadata`。

## 14. RLS 权限原则

- 成员只能读取自己所属友间的数据；
- 成员只能操作自己的进行中专注记录；
- 成员不能直接更新累计时长和结算字段；
- 所有成员都能读取同一友间内其他成员的任务名称和实时专注状态；
- 房主才能重置邀请链接或停用成员；
- 任何客户端都不能更新或删除 `focus_events`；
- Supabase `service_role` 密钥绝不能出现在前端代码中。

## 15. MVP 页面范围

### 15.1 加入页

- 显示友间名称；
- 输入昵称；
- 提示匿名身份绑定当前设备且无法恢复；
- 加入按钮。

### 15.2 友间首页

- 当前正在专注的成员；
- 每人的任务和累计时长；
- 当前用户的开始／暂停／恢复／结束控制；
- 今日个人累计；
- 当前连续打卡天数。

### 15.3 开始专注面板

- 任务名称；
- 可选分类；
- 开始按钮。

### 15.4 统计页

- 个人日、周、月累计；
- 小组日、周、月累计；
- 连续打卡；
- 历史专注记录；
- 连接状态不可确认标记。

### 15.5 共同目标页

- 当前生效目标；
- 历史目标；
- 发起提案；
- 接受或拒绝待处理提案。

### 15.6 设置页

- 友间信息；
- 成员列表；
- 匿名身份丢失警告；
- PWA 安装说明；
- 房主专属的邀请重置和成员停用功能。

## 16. 明确不属于 MVP

- TickTick／滴答清单数据同步；
- 完整任务管理、子任务或清单；
- 聊天、语音或视频；
- 公共房间和陌生人匹配；
- 排行榜；
- 多设备同步；
- 匿名身份恢复；
- 推送通知；
- 手动补录或修正时长；
- 修改或删除历史记录；
- 原生 iOS／Android App；
- 复杂可视化和社交动态。

## 17. MVP 验收标准

### 身份与房间

- 两台不同设备可通过同一链接以不同昵称加入；
- 同一友间不能出现重复的活动昵称；
- 非成员无法读取友间数据；
- 房主可以重置邀请链接和停用成员。

### 计时

- 开始、暂停、恢复和结束状态正确同步；
- 暂停时间不计入累计；
- 暂停满 15 分钟自动按暂停时刻结算；
- 实际专注满 6 小时自动结算；
- 单次少于 5 分钟保留但不计入统计；
- 已结算记录不能恢复、修改或删除；
- 同一用户不能并行开启两条记录。

### 实时与异常

- 好友可在数秒内看到开始、暂停、恢复和结束变化；
- 手机锁屏或网页关闭后服务器计时继续；
- 恢复连接后显示与服务器结果一致；
- 连接状态不可确认区间被保留并按分钟显示。

### 统计

- 跨午夜记录按时区正确拆分；
- 日、周、月累计排除暂停时间和无效记录；
- 个人统计使用个人时区；
- 共同统计使用房间时区；
- 连续打卡按照最终配置的每日门槛计算。

### 共同目标

- 任一成员可发起三种支持的目标；
- 所有当前成员一致接受后才通过；
- 拒绝不会修改原提案；
- 48 小时未通过自动过期；
- 目标只从下一个完整周期开始生效。

## 18. 已冻结产品决策

### 每日打卡门槛

每天累计 60 分钟完成个人打卡。写入友间配置：

```text
daily_checkin_target_minutes = 60
```

### 单个身份的友间数量

MVP 正式限制为一个匿名身份只能加入一个友间。数据库结构预留未来多友间扩展能力，但服务端必须拒绝第二个活动成员关系，避免首版引入房间切换、专注归属和跨房间同时可见性等额外规则。

完整页面状态、交互和异常处理见 [UI_STATE_SPEC.md](./UI_STATE_SPEC.md)。MVP 核心业务规则现已冻结，可以进入实现阶段。
