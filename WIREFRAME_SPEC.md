# 友间（Youjian）MVP 线框与组件规格

> 依赖：[MVP_SPEC.md](./MVP_SPEC.md)、[UI_STATE_SPEC.md](./UI_STATE_SPEC.md)  
> 目标：在不冻结最终配色和插画的前提下，确定页面层级、核心组件和状态变化。  
> 已冻结规则：每日打卡 60 分钟；一个匿名身份只加入一个友间。

## 1. 画布与布局基准

### 移动端

- 基准画布：390×844；
- 支持最小宽度：320px；
- 页面左右安全边距：16px；
- 卡片间距：12px；
- 底部导航高度：64px，加设备安全区；
- 主操作按钮高度：52px；
- 页面内容底部至少预留 88px，避免被导航遮挡。

### 桌面端

- 断点：`>= 768px`；
- 左侧导航：88px；
- 主内容最大宽度：960px；
- 首页双栏：我的专注 5/12，好友状态 7/12；
- 统计与目标保持单主栏，不做密集后台式布局。

### 信息优先级

1. 自己当前是否正在计时；
2. 好友是否正在专注及任务内容；
3. 今日累计与打卡；
4. 共同目标；
5. 历史和设置。

## 2. 全局应用外壳

### 移动端

```text
┌──────────────────────────┐
│ 友间名              状态点 │  顶栏 52
├──────────────────────────┤
│                          │
│       当前页面内容        │
│                          │
│                          │
├──────────────────────────┤
│ 首页   统计   目标   设置  │  底栏 64 + safe area
└──────────────────────────┘
```

- 顶栏跟随内容滚动，不强制固定；
- 底栏固定；
- 连接正常时状态点不显示，异常时才出现带文字入口；
- 页面标题最多一行，超长省略。

### 桌面端

```text
┌────────┬───────────────────────────────────┐
│ 友间   │ 友间名                     状态点 │
│        ├───────────────────────────────────┤
│ 首页   │                                   │
│ 统计   │             页面内容              │
│ 目标   │                                   │
│ 设置   │                                   │
│        │                                   │
└────────┴───────────────────────────────────┘
```

## 3. 欢迎页

```text
┌──────────────────────────┐
│                          │
│          ◯ 灯            │
│          友间             │
│     在友间，自有间。       │
│                          │
│ 和熟悉的人共享专注状态，    │
│ 不接管你的任务。           │
│                          │
│  [    创建一个友间     ]   │
│                          │
│      通过邀请链接加入       │
│                          │
└──────────────────────────┘
```

布局规则：

- 品牌灯光区域占首屏上部约 35%；
- 只有一个主按钮；
- “通过邀请链接加入”只负责说明，实际加入通常由用户直接打开链接；
- 已有有效身份时，主按钮变为“回到友间”。

## 4. 创建友间

```text
┌──────────────────────────┐
│ ← 创建友间                │
├──────────────────────────┤
│ 你的昵称                  │
│ [ 例如：赵琳             ] │
│                          │
│ 友间名称                  │
│ [ 我们的友间             ] │
│                          │
│ 房间时区                  │
│ [ Asia/Shanghai        ▾ ] │
│                          │
│ 人数上限                  │
│ [  2  ] [  3  ] [ 其他 ▾ ]│
│                          │
│ ┌──────────────────────┐ │
│ │ □ 身份只保存在当前设备 │ │
│ │   清除数据后无法恢复…  │ │
│ └──────────────────────┘ │
│                          │
│ [      创建友间         ] │
└──────────────────────────┘
```

- 人数上限优先露出 2、3；其他选项展开后为 4～12；
- 身份说明必须完整可读，不藏进条款链接；
- 提交中保留表单内容，按钮替换为“正在创建…”。

## 5. 邀请加入

### 正常

```text
┌──────────────────────────┐
│                          │
│        一盏等待的灯        │
│                          │
│     加入「我们的友间」      │
│    由 陈宇 创建 · 2/3 人   │
│      Asia/Shanghai       │
│                          │
│ 你的昵称                  │
│ [                      ] │
│                          │
│ ┌──────────────────────┐ │
│ │ 身份只保存在当前设备，  │ │
│ │ 更换设备后无法恢复。    │ │
│ └──────────────────────┘ │
│                          │
│ [       加入友间        ] │
└──────────────────────────┘
```

### 失效／已满／停用

统一使用结果页骨架：

```text
┌──────────────────────────┐
│                          │
│           图标            │
│       这个邀请已失效       │
│ 请让房主发送新的邀请链接。  │
│                          │
│ [       返回首页         ] │
└──────────────────────────┘
```

不同结果只替换图标、标题、说明和允许操作，不保留无效表单。

## 6. 首页：空闲

```text
┌──────────────────────────┐
│ 我们的友间                │
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │ 今天  38 分钟          │ │
│ │ ███████░░░  距打卡22分 │ │
│ │ 连续 6 天              │ │
│ └──────────────────────┘ │
│                          │
│ ┌──────────────────────┐ │
│ │         ◯             │ │
│ │      点亮你的台灯      │ │
│ │                      │ │
│ │ [    开始专注       ] │ │
│ └──────────────────────┘ │
│                          │
│ 正在亮灯                  │
│ ┌──────────────────────┐ │
│ │ 赵琳                  │ │
│ │ 体育法阅读             │ │
│ │ 阅读 · 36 分钟         │ │
│ └──────────────────────┘ │
│                          │
│ 本周共同目标       查看 >  │
│ 12小时 / 20小时           │
├──────────────────────────┤
│ 首页   统计   目标   设置  │
└──────────────────────────┘
```

确定规则：

- 今日摘要在计时卡片之前，提供长期反馈但不抢走主操作；
- “正在亮灯”卡片按开始时间排序，先开始的人在前；
- 成员超过 4 人时首页只显示前 4 位，提供“查看全部正在专注（N）”；
- 没有共同目标时显示一行轻入口，不占大卡片。

## 7. 开始专注抽屉

```text
┌──────────────────────────┐
│      页面背景变暗 20%      │
│                          │
├──────────────────────────┤
│ 开始一段专注          ×    │
│                          │
│ 这次要做什么？             │
│ [ 体育法阅读             ] │
│                     6/80 │
│                          │
│ 分类（可选）               │
│ [学习] [工作] [阅读]       │
│ [运动] [其他]              │
│                          │
│ [       点亮台灯        ]  │
└──────────────────────────┘
```

- 移动端使用底部抽屉，桌面端使用宽 480px 居中对话框；
- 打开后自动聚焦任务输入；
- 键盘弹出时主按钮跟随内容滚动，不固定在键盘上方遮挡输入；
- 点击遮罩可关闭，但提交中不可关闭。

## 8. 首页：正在专注

```text
┌──────────────────────────┐
│ 我们的友间                │
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │      ╭────────╮       │ │
│ │      │ 亮灯中 │       │ │
│ │      ╰────────╯       │ │
│ │                      │ │
│ │ 体育法阅读             │ │
│ │ 阅读                   │ │
│ │       00:36:18        │ │
│ │                      │ │
│ │ [      暂停         ] │ │
│ │        结束本次        │ │
│ └──────────────────────┘ │
│                          │
│ 正在亮灯 · 1              │
│ [  陈宇  法语论文  24分钟 ]│
│                          │
│ 今天累计 1小时14分 · 已打卡 │
├──────────────────────────┤
│ 首页   统计   目标   设置  │
└──────────────────────────┘
```

- 自己的任务卡成为页面视觉中心；
- 时长精确到秒，统计摘要精确到分钟；
- “结束本次”使用文字按钮，避免和暂停同权重；
- 距离 6 小时不足 30 分钟时，在时长下增加橙色说明。

## 9. 首页：暂停

```text
┌──────────────────────────┐
│ 我们的友间                │
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │        灯光收暗        │ │
│ │      暂时离开          │ │
│ │                      │ │
│ │ 体育法阅读             │ │
│ │ 已专注 36 分钟          │ │
│ │                      │ │
│ │      14:32 后结束      │ │
│ │                      │ │
│ │ [     继续专注       ] │ │
│ │        结束本次        │ │
│ └──────────────────────┘ │
│                          │
│ 正在亮灯 · 1              │
│ [  陈宇  法语论文  24分钟 ]│
├──────────────────────────┤
│ 首页   统计   目标   设置  │
└──────────────────────────┘
```

- 自己暂停不影响查看好友；
- 其他成员看不到自己暂停卡片；
- 剩余一分钟时只改变倒计时强调色；
- 倒计时归零后卡片进入“等待服务器确认”，不先展示成功。

## 10. 首页：连接异常

### 正在专注时

```text
┌──────────────────────────┐
│ ⚠ 连接状态不可确认         │
│   计时仍继续 · 正在重连     │
├──────────────────────────┤
│                          │
│       体育法阅读           │
│        00:42:09           │
│                          │
│ [      暂停           ]   │
│        结束本次            │
│                          │
│ 操作需要连接服务器后生效。  │
└──────────────────────────┘
```

点击暂停或结束但请求失败时，在控制区内展开错误，不叠加多个 Toast：

> 这次操作还没有生效，专注计时仍在继续。连接恢复后请重试。

### 好友状态不可确认

成员卡片不变灰、不移除，只增加小型橙色状态：

```text
赵琳
体育法阅读 · 36 分钟
◷ 最后同步于 14:32
```

## 11. 结算结果

使用首页内结果卡替换计时卡，不跳转新页面。

```text
┌──────────────────────────┐
│          ✓               │
│      完成一段专注          │
│                          │
│       体育法阅读           │
│        42 分钟            │
│                          │
│ [      再来一段        ]  │
│        查看记录           │
└──────────────────────────┘
```

变体：

- 主动结束：绿色确认；
- 暂停超时：中性说明并标注自动结束；
- 六小时上限：中性说明；
- 少于 5 分钟：灰色说明“不计入统计”，不使用失败红色。

结果卡在用户离开首页或再次开始后消失，不永久占位。

## 12. 统计页

```text
┌──────────────────────────┐
│ 统计                      │
├──────────────────────────┤
│ [ 我的 ] [ 友间 ]          │
│                          │
│ [ 日 ] [ 周 ] [ 月 ]       │
│                          │
│ 本周专注                  │
│      8 小时 24 分          │
│ 12 次有效专注 · 5 个打卡日  │
│                          │
│ 一  二  三  四  五  六  日  │
│ ●  ●  ●  ○  ●  ○  ○      │
│                          │
│ 历史记录                  │
│ ┌──────────────────────┐ │
│ │ 体育法阅读      42分钟 │ │
│ │ 今天 14:00–14:42 阅读 │ │
│ └──────────────────────┘ │
│ ┌──────────────────────┐ │
│ │ 单词复习         3分钟 │ │
│ │ 不计入统计             │ │
│ └──────────────────────┘ │
├──────────────────────────┤
│ 首页   统计   目标   设置  │
└──────────────────────────┘
```

- “我的／友间”和“日／周／月”均使用分段控件；
- 不使用饼图或成员排名；
- 首版日历只表达是否打卡，不用深浅颜色比较时长；
- 历史详情用底部抽屉展开，不建立独立路由。

### 历史详情抽屉

```text
任务：体育法阅读
分类：阅读
开始：7月27日 14:00
结束：7月27日 14:42
实际专注：42分钟
结算：主动结束
连接：包含约3分钟状态不可确认区间
```

没有编辑、删除、复制时长或补录入口。

## 13. 目标页

### 有生效目标

```text
┌──────────────────────────┐
│ 共同目标             ＋    │
├──────────────────────────┤
│ 当前目标                  │
│ ┌──────────────────────┐ │
│ │ 本周共同专注 20 小时    │ │
│ │ ██████░░░░ 12 / 20h   │ │
│ │ 周一开始 · 周日结束     │ │
│ └──────────────────────┘ │
│                          │
│ 等待决定                  │
│ ┌──────────────────────┐ │
│ │ 每人每周至少 5 小时     │ │
│ │ 赵琳提出 · 还剩31小时   │ │
│ │ 1 / 3 人已接受          │ │
│ │ [接受]       [拒绝]     │ │
│ └──────────────────────┘ │
│                          │
│ 即将生效 / 历史目标        │
├──────────────────────────┤
│ 首页   统计   目标   设置  │
└──────────────────────────┘
```

### 无目标

```text
还没有共同目标。
你们可以约定累计时长、每人门槛或共同打卡天数。
[ 提出第一个目标 ]
```

## 14. 创建目标

采用单页逐段展开，不做三张独立页面。

```text
┌──────────────────────────┐
│ ← 提出共同目标             │
├──────────────────────────┤
│ 1. 想一起做到什么？         │
│ (●) 小组合计               │
│ ( ) 每人门槛               │
│ ( ) 共同出勤               │
│                          │
│ 2. 按什么周期？             │
│ [每日] [每周] [每月]        │
│                          │
│ 3. 目标值                  │
│ [ 20 ] 小时               │
│                          │
│ ┌──────────────────────┐ │
│ │ 从下周一开始，本友间每 │ │
│ │ 周共同专注20小时……     │ │
│ └──────────────────────┘ │
│                          │
│ [       提交提案        ]  │
└──────────────────────────┘
```

- 类型变化时同步改变目标单位；
- 预览必须写成人类可读的完整句子；
- 目标值采用合理上下限，防止 0、负数或极端值；
- 提交成功后返回目标页并定位到“等待决定”。

## 15. 设置页

```text
┌──────────────────────────┐
│ 设置                      │
├──────────────────────────┤
│ 友间                      │
│ 我们的友间                │
│ Asia/Shanghai · 2/3人    │
│                          │
│ 成员                      │
│ 陈宇                 房主 │
│ 赵琳                 成员 │
│                          │
│ 邀请                      │
│ [      复制邀请链接      ] │
│        重置邀请链接        │
│                          │
│ 当前设备                  │
│ 昵称：陈宇                │
│ 身份无法跨设备恢复          │
│                          │
│ 安装                      │
│ [      添加到桌面        ] │
│                          │
│ 关于友间 · 隐私 · v0.1     │
├──────────────────────────┤
│ 首页   统计   目标   设置  │
└──────────────────────────┘
```

- 普通成员不显示重置邀请和停用入口；
- 房主点击成员行打开管理抽屉；
- 匿名身份警告始终可见，不折叠进“高级设置”；
- 不提供退出登录按钮，避免用户误删唯一身份；清除身份属于浏览器／系统行为。

## 16. 破坏性确认框

### 重置邀请链接

```text
重置邀请链接？
之前发送的链接将立即失效，已加入成员不受影响。

[取消] [重置链接]
```

### 停用成员

```text
停用赵琳？
她将无法继续进入这个友间。历史记录仍会保留，且无法转移或删除。

[取消] [停用成员]
```

- 取消始终位于左侧／上方；
- 危险操作使用红色但不默认获得焦点；
- 处理中两个按钮均禁用；
- 失败后保留对话框并显示具体错误。

## 17. 全局骨架屏与错误页

### 首页骨架

只模拟今日摘要、我的专注卡和两个成员卡，不模拟真实数字，防止用户误认缓存为当前状态。

### 可恢复错误

```text
部分内容暂时没有更新
上次数据仍保留，计时状态正在重新确认。
[重新加载]
```

### 致命错误

```text
当前无法进入友间
你的身份已失效或没有访问权限。
[返回首页]
```

错误码放在折叠的“技术信息”中，便于排查但不作为主文案。

## 18. 组件清单

### 基础组件

- `Button`：primary、secondary、text、danger；
- `TextField`；
- `Select`；
- `SegmentedControl`；
- `ProgressBar`；
- `StatusBadge`；
- `InlineNotice`；
- `Banner`；
- `Toast`；
- `Dialog`；
- `BottomSheet`；
- `Skeleton`；
- `EmptyState`。

### 业务组件

- `TodaySummary`；
- `FocusControlCard`；
- `MemberFocusCard`；
- `ConnectionBanner`；
- `SessionResultCard`；
- `HistorySessionRow`；
- `GoalProgressCard`；
- `GoalProposalCard`；
- `AchievementCard`；
- `MemberManagementSheet`；
- `IdentityWarning`；
- `PwaInstallCard`。

组件不得自行计算权威业务结果；传入的状态应来自统一领域状态层。

## 19. 响应式变化

| 区域 | 移动端 | 桌面端 |
|---|---|---|
| 导航 | 底部四项 | 左侧四项 |
| 首页 | 单列 | 我的专注与好友双栏 |
| 抽屉 | 底部抽屉 | 居中对话框 |
| 成员卡 | 单列 | 最多两列 |
| 统计筛选 | 顶部分段控件 | 同一行靠左 |
| 目标卡 | 单列 | 单列，最大宽 720px |
| 设置 | 单列分组 | 两栏分组，危险操作仍单独区域 |

桌面端不增加移动端没有的业务能力，只改变排布。

## 20. 原型状态集

制作可点击原型时至少建立以下 16 个画面：

1. 欢迎；
2. 创建友间；
3. 正常邀请；
4. 邀请失效；
5. 首页空闲且好友专注；
6. 开始专注抽屉；
7. 自己正在专注；
8. 自己暂停；
9. 连接状态不可确认；
10. 正常结算；
11. 不足 5 分钟结算；
12. 统计正常；
13. 统计空状态；
14. 目标正常及待投票；
15. 创建目标；
16. 房主设置及停用确认。

这些画面覆盖主流程、自动结算前状态、关键异常和高风险管理操作。

## 21. 线框验收

- [ ] 任意页面最多有一个视觉主按钮；
- [ ] 三秒内能定位自己的计时状态；
- [ ] 好友任务不需要进入二级页面即可看到；
- [ ] 暂停剩余时间及后果同时可见；
- [ ] 断网时不会出现虚假的暂停／结束成功；
- [ ] 不足 5 分钟的结算不使用失败语气；
- [ ] 统计不包含排行榜式竞争；
- [ ] 共同目标提交前可读地预览完整规则；
- [ ] 匿名身份风险在创建、加入和设置三个位置可见；
- [ ] 房主危险操作均有明确后果和二次确认；
- [ ] 320px 宽度下不出现关键内容横向滚动；
- [ ] 桌面端不因空白过多而拉宽计时卡；
- [ ] 所有核心流程可仅用键盘完成。

## 22. 已冻结决策的线框落实

### 每日打卡门槛

线框统一按 60 分钟展示。未来产品版本如调整配置，只需替换：

- `TodaySummary` 的目标和剩余时间；
- 打卡日历判定；
- 共同出勤目标说明；
- 相关成就条件。

布局无需变化。

### 多友间

MVP 线框按一个身份一个友间设计。未来版本若允许多个友间，需要新增：

- 顶栏友间切换器；
- 欢迎页的友间列表；
- 加入第二个友间后的去向；
- 计时记录所属友间提示；
- 设置页的退出某个友间规则；
- 是否允许一条专注同时广播到多个友间的业务决定。

因此，多友间不是纯数据库开关，会改变主导航和计时语义。
