# 友间（Youjian）MVP 页面与状态设计规格

> 依赖文档：[API_CONTRACT.md](./API_CONTRACT.md)、[TECHNICAL_DESIGN.md](./TECHNICAL_DESIGN.md)
> 文档状态：可供线框图、前端状态建模和验收测试使用  
> 设计原则：移动端优先、服务端权威、状态透明、操作克制。

## 1. 设计目标

这份文档定义的不只是页面长相，而是用户在正常、等待、失败、离线、自动结算和权限变化时具体看到什么、能做什么，以及系统如何恢复。

核心体验目标：

- 三秒内看懂谁正在专注、正在做什么；
- 一次点击开始、暂停、恢复或结束；
- 所有可能影响计时结果的状态都明确反馈；
- 不把尚未提交到服务器的操作伪装成成功；
- 不用复杂社交功能干扰专注；
- 让连接异常可见，但不把它等同于作弊或真实断网。

## 2. 信息架构

### 2.1 顶层页面

```text
启动
├── 欢迎页
├── 创建友间
├── 邀请加入
└── 已有身份恢复入口
    └── 友间
        ├── 首页
        ├── 统计
        ├── 目标
        └── 设置
```

“已有身份恢复入口”只表示恢复浏览器中仍存在的匿名会话，不提供跨设备或清除数据后的身份恢复。

### 2.2 移动端导航

底部固定四项：

1. 首页；
2. 统计；
3. 目标；
4. 设置。

计时操作始终属于首页，不增加独立“计时器”导航项。正在专注或暂停时，离开首页不会终止计时。

### 2.3 桌面端导航

使用左侧窄栏承载相同四项。内容最大宽度建议为 960px，避免为十几位成员制作企业仪表盘式密集布局。

## 3. 全局状态原则

### 3.1 服务端权威

- 服务器确认前，不更新最终业务状态；
- 提交期间按钮进入加载态并防重复点击；
- 服务器返回后，以服务器记录覆盖本地推算；
- 客户端只使用服务器时间基准平滑显示计时；
- 客户端不得离线补报“我在某时刻暂停／结束”。

### 3.2 全局反馈层级

| 级别 | 用途 | 展现方式 | 示例 |
|---|---|---|---|
| 页面状态 | 页面无法正常使用 | 整页说明 | 邀请失效、成员被停用 |
| 横幅 | 页面仍可使用但存在风险 | 顶部常驻条 | 连接状态不可确认 |
| 行内反馈 | 局部输入或操作异常 | 控件附近 | 昵称已被使用 |
| Toast | 操作已完成且无需决策 | 3 秒自动消失 | 邀请链接已复制 |
| 对话框 | 不可逆或高影响操作 | 阻断确认 | 停用成员、重置邀请 |

### 3.3 通用异步状态

每个需要网络的页面必须覆盖：

- `initial_loading`：首次载入，用骨架屏；
- `refreshing`：保留旧内容，局部显示刷新指示；
- `success`：正常内容；
- `empty`：明确说明为什么为空；
- `recoverable_error`：保留可用旧数据并提供“重新加载”；
- `fatal_error`：无法识别身份或无权访问，转到整页错误；
- `stale`：展示缓存内容，同时标记“数据可能不是最新”。

禁止只显示无限旋转的 loading。任何请求超过 10 秒后必须转为可重试状态。

## 4. 视觉状态语言

本规格不冻结最终品牌视觉，但冻结状态语义：

- **暖黄色灯光**：正在专注；
- **柔和蓝灰色**：暂停；
- **中性灰色**：空闲或历史；
- **橙色**：连接状态不可确认、即将自动结算；
- **绿色**：打卡或目标达成；
- **红色**：破坏性操作、明确失败；
- 不只依赖颜色，同时使用图标、文字和形状表达状态；
- 所有计时数字使用等宽数字，防止跳动造成布局位移。

品牌意象“一盏灯圈出一块圆形的光”用于正在专注状态，不用于错误或警告。

## 5. 欢迎页与创建友间

### 5.1 欢迎页

主要内容：

- 友间名称；
- Slogan“在友间，自有间。”；
- 一句话说明“和熟悉的人共享专注状态，不接管你的任务”；
- 主按钮“创建一个友间”；
- 次入口“通过邀请链接加入”。

状态：

| 状态 | 页面行为 |
|---|---|
| 未登录 | 正常显示创建入口 |
| 匿名身份仍有效且已有友间 | 主按钮改为“回到友间” |
| 匿名身份仍有效但没有友间 | 正常显示创建入口 |
| 离线首次访问 | 禁用创建和加入，提示“连接网络后才能创建或加入” |
| PWA 缓存可用 | 可展示欢迎页，但不伪造房间数据 |

### 5.2 创建友间表单

字段：

- 你的昵称：必填，1～20 字；
- 友间名称：必填，1～30 字，默认建议“我们的友间”；
- 房间时区：默认取设备 IANA 时区；
- 人数上限：默认 3，可选 2～12；
- 匿名身份说明复选确认：必选。

身份说明固定文案：

> 你的身份只保存在当前设备。清除浏览器数据、更换设备或删除 PWA 数据后将无法恢复，历史记录也不能转移。

表单状态：

- 初始；
- 字段非法；
- 正在创建；
- 创建成功；
- 创建失败；
- 匿名认证创建失败；
- 重复提交被拦截。

成功后直接进入首页，并用一次性提示引导房主复制邀请链接。

## 6. 邀请加入页

### 6.1 正常状态

显示：

- 友间名称；
- 房主昵称；
- 当前成员数／上限；
- 房间时区；
- 昵称输入框；
- 身份不可恢复说明；
- 主按钮“加入友间”。

不公开展示其他成员的任务和历史数据，加入成功后才可读取。

### 6.2 邀请状态矩阵

| 状态 | 标题 | 说明 | 可执行操作 |
|---|---|---|---|
| 有效 | 加入「友间名」 | 输入你在这里使用的昵称 | 加入 |
| 链接失效 | 这个邀请已失效 | 请让房主发送新的邀请链接 | 无 |
| 房间已满 | 这里已经坐满了 | 当前人数已达到房主设置的上限 | 返回欢迎页 |
| 昵称为空 | — | 请输入昵称 | 不提交 |
| 昵称占用 | 这个昵称已有人使用 | 换一个昵称后再加入 | 修改昵称 |
| 当前设备已加入 | 你已经在这个友间里 | 无需重复加入 | 回到友间 |
| 成员已停用 | 你已不能进入这个友间 | 历史记录仍会保留 | 返回欢迎页 |
| 加入提交中 | 正在为你留座 | 禁止重复提交 | 等待 |
| 网络失败 | 暂时没能加入 | 未产生成员身份 | 重试 |

“网络失败”必须在服务器确认成员是否已创建后再允许重试，重试请求必须具有幂等性。

## 7. 首页整体结构

从上到下：

1. 顶栏：友间名称、连接状态入口；
2. 今日摘要：个人累计、距离打卡目标、连续天数；
3. 我的专注控制区；
4. 正在亮灯：其他正在专注的成员；
5. 当前共同目标摘要；
6. 底部导航。

首页不展示完整历史、排行榜或聊天流。

## 8. 我的专注状态机

### 8.1 状态定义

| UI 状态 | 服务端依据 | 主操作 | 次操作 |
|---|---|---|---|
| `idle` | 无未完成记录 | 开始专注 | 无 |
| `starting` | 开始请求未返回 | 正在开始… | 无 |
| `focusing` | session=`focusing` | 暂停 | 结束 |
| `pausing` | 暂停请求未返回 | 正在暂停… | 无 |
| `paused` | session=`paused` | 继续专注 | 结束本次 |
| `resuming` | 恢复请求未返回 | 正在恢复… | 无 |
| `ending` | 结束请求未返回 | 正在结算… | 无 |
| `reconciling` | 本地状态与服务器待核对 | 正在同步 | 无 |
| `completed_notice` | 刚完成有效记录 | 再来一段 | 查看记录 |
| `discarded_notice` | 刚完成且少于 5 分钟 | 重新开始 | 查看记录 |

任何时刻只允许一个状态生效。按钮在过渡状态中全部防重复点击。

### 8.2 `idle`

显示：

- 灯处于未点亮状态；
- 主按钮“开始专注”；
- 今日累计与打卡进度；
- 最近一次任务可作为弱提示，但不得自动填入或一键重复开始。

点击后打开“开始专注”底部抽屉，不直接开始，确保任务内容必填。

### 8.3 开始专注抽屉

字段：

- 任务名称，1～80 字；
- 分类，可选固定值：学习、工作、阅读、运动、其他；
- 主按钮“点亮台灯”。

校验：

- 去除首尾空格后为空：提示“写下这次要做的事情”；
- 超过 80 字：显示剩余字数为负并禁用提交；
- 分类不影响计时有效性；
- 提交后立即锁定输入，防止重复提交。

### 8.4 `focusing`

显示：

- 已点亮的灯光状态；
- 任务名称；
- 分类；
- 本次累计专注时长；
- “暂停”主按钮；
- “结束”次按钮；
- 距离 6 小时上限的提示仅在剩余 30 分钟内出现。
- “修改任务”按钮；保存成功后显示最新版和“已修改”入口，点击可展开按时间倒序排列的旧任务便签。

计时显示每秒更新，但统计和结算仍以服务端数据为准。

点击“结束”时：

- 本次不足 5 分钟：弹出确认框，说明“结束后这段记录会保留，但不计入统计”；
- 本次达到 5 分钟：直接进入 `ending`，不增加确认步骤；
- 结束失败：返回服务器确认的 `focusing` 状态，并显示行内错误。

### 8.5 `paused`

显示：

- 灯光收暗；
- 标题“暂时离开”；
- 已累计专注时长保持不变；
- 15 分钟倒计时；
- 主按钮“继续专注”；
- 次按钮“结束本次”。
- “修改任务”按钮；倒计时归零后禁用，避免修改已经到期的 session。

倒计时文案：

> 14:32 后将自动结束，本次专注会结算到暂停时刻。

剩余 1 分钟时变为橙色，但不播放声音、不振动、不发送推送。

达到 0 后立即进入 `reconciling`，请求服务器状态；服务器确认后显示自动结算结果。不能在客户端自行恢复。

### 8.6 自动结算结果

#### 暂停超时

> 因暂停超过 15 分钟，本次已自动结束。已计入 42 分钟专注。

#### 六小时上限

> 本次已达到 6 小时上限并自动结束。长时间专注后请适当休息。

#### 自动结算但不足 5 分钟

> 本次不足 5 分钟，记录已保留，但不会计入统计和打卡。

所有结果页提供“再来一段”和“查看记录”，不提供恢复或编辑。

## 9. 其他成员状态

“正在亮灯”区域只展示处于 `focusing` 的其他成员。

成员卡片显示：

- 昵称；
- 任务名称；
- 分类；
- 本次累计专注时长；
- 连接状态不可确认提示（如有）。
- 任务有修改历史时显示“已修改”；友间成员可展开查看旧任务名、旧分类和修改时间。

确定的展示规则：

- 好友暂停后，从“正在亮灯”列表移除；
- 不向其他成员展示“上厕所／休息中”等状态；
- 好友恢复后重新出现，累计时间不包含暂停时间；
- 好友结束后卡片平滑淡出；
- 不显示精确上线／离线状态；
- 不允许点击成员卡片进入个人档案，避免扩大社交范围。

空状态：

> 现在还没有人亮灯。你可以先开始，朋友打开友间后就能看到。

自己正在专注但没有其他人时：

> 你的灯已经亮了。朋友加入后会在这里出现。

## 10. 连接与同步状态

### 10.1 连接状态模型

| 状态 | 判定 | 页面表现 |
|---|---|---|
| `connected` | 请求和 Realtime 正常 | 不显示横幅 |
| `realtime_degraded` | API 可用但实时订阅中断 | “实时更新暂时中断，正在重连” |
| `unconfirmed` | 心跳超时或应用从后台恢复 | “连接状态不可确认，计时仍在继续” |
| `offline` | 浏览器报告离线且请求失败 | “当前离线，计时仍在服务器端继续” |
| `reconnecting` | 网络恢复，正在核对 | “正在同步最新状态…” |
| `conflict` | 本地推算与服务器状态不同 | 阻止操作，完成核对后解释结果 |

浏览器的 `navigator.onLine` 只作为提示，不作为真实断网证据。

### 10.2 离线时的操作

正在专注时仍显示“暂停”和“结束”，但点击后不做离线排队，而是显示：

> 当前无法连接服务器，这次操作还没有生效，专注计时仍在继续。连接恢复后请重试。

这样避免恢复网络后按旧时间补交操作。

暂停状态下离线时：

- 本地继续显示预计倒计时；
- 到达 0 后不宣称已经结算；
- 显示“正在等待服务器确认”；
- 恢复连接后获取真实结果。

### 10.3 从后台返回

依次执行：

1. 冻结所有状态切换按钮；
2. 显示“正在同步最新状态”；
3. 获取服务器当前时间与 session；
4. 替换本地推算；
5. 如已自动结算，展示对应结算说明；
6. 如仍在专注或暂停，恢复正常控制。

### 10.4 好友连接不可确认

好友仍处于 `focusing` 时继续显示卡片，并附加：

> 连接中断或应用在后台 · 最后同步于 14:32

恢复后提示自动消失。历史页显示该次记录包含的近似未确认分钟数。

## 11. 今日摘要与连续打卡

### 11.1 今日未完成

显示：

- 今日已累计时长；
- 每日目标；
- 尚差时长；
- 当前连续打卡天数。

文案模板：

> 今天已专注 38 分钟，再专注 {remaining} 即可完成打卡。

### 11.2 今日完成

显示绿色完成状态：

> 今日已打卡 · 连续 {streak} 天

超过门槛后继续累计真实时长，不显示超过百分比，不把持续专注变成无限增长的进度压力。

### 11.3 连续中断

不使用惩罚性文案。显示：

> 上一次连续打卡为 6 天。今天完成后会开启新的连续记录。

### 11.4 同日共同打卡

当房间时区内所有活动成员都完成当天打卡时，首页出现一次性轻量动画：多盏台灯形成共同光圈。动画不超过 1.5 秒，可跳过，且遵循系统“减少动态效果”设置。

## 12. 统计页

### 12.1 页面结构

- 视角切换：我的／友间；
- 周期切换：日／周／月；
- 总专注时长；
- 有效专注次数；
- 打卡日历或周期进度；
- 历史记录列表。

MVP 不做排行榜，不默认比较成员高低。

### 12.2 统计状态

| 状态 | 展现 |
|---|---|
| 无任何记录 | “还没有专注记录，从点亮第一盏灯开始。” |
| 只有无效记录 | 显示记录，并说明“少于 5 分钟，不计入统计” |
| 正常 | 展示聚合与历史 |
| 当前周期无数据 | 周期总计为 0，不复用上期数据 |
| 包含跨日记录 | 历史详情展示按日期拆分结果 |
| 包含未确认区间 | 记录旁显示中性连接标记 |
| 加载失败 | 保留旧数据并显示重新加载 |

### 12.3 历史记录项

显示：

- 任务名称；
- 分类；
- 开始与结束时间；
- 实际专注时长；
- 结算方式；
- 是否不计入统计；
- 是否包含连接状态不可确认区间。

记录不可侧滑删除，不显示编辑按钮。

结算文案映射：

| 原因 | 文案 |
|---|---|
| `manual_end` | 主动结束 |
| `pause_timeout` | 暂停超过 15 分钟，自动结束 |
| `focus_limit` | 达到 6 小时上限，自动结束 |

### 12.4 时区说明

个人视角显示“按你的时区统计”；友间视角显示具体房间时区。跨时区时不隐去差异。

## 13. 共同目标页

### 13.1 页面分区

1. 当前生效目标；
2. 等待决定的提案；
3. 即将生效；
4. 历史目标；
5. “提出共同目标”按钮。

### 13.2 创建提案

分三步完成：

1. 选择类型：小组合计／个人门槛／共同出勤；
2. 选择周期：每日／每周／每月；
3. 填写目标值并预览完整句子。

预览示例：

> 若今天全员通过，将从明天 00:00 开始，连续 7 天内每位成员至少专注 5 小时。需要当前 3 位成员全部接受后生效。

提交前明确显示：

- 生效日期；
- 当前需同意人数；
- 48 小时过期时间；
- 提案提交后不能编辑。

同一时间只能有一个待投票、待开始或进行中的共同目标。目标参与者以提案创建时的投票成员快照为准；之后加入的人不追加到该目标。最终开始日期在最后一票通过时按友间时区重算为次日 00:00。

### 13.3 提案状态

| 状态 | 卡片内容 | 当前用户操作 |
|---|---|---|
| `pending_unvoted` | 提案、发起人、剩余时间、投票进度 | 接受／拒绝 |
| `pending_accepted_by_me` | “你已接受，等待其他成员” | 无 |
| `accepted` | “全员已接受，将于…生效” | 无 |
| `rejected` | 显示拒绝结果，不公开要求填写理由 | 发起新提案 |
| `expired` | “48 小时内未获得全员接受” | 发起新提案 |
| `active` | 当前进度、周期剩余时间 | 无 |
| `completed` | 达成时间和最终结果 | 无 |
| `failed` | 最终进度和未达成说明 | 无 |

接受操作无需二次确认。拒绝操作需要简短确认：

> 拒绝后这份提案将结束。如需调整，请创建新的提案。

### 13.4 目标进度

- 小组合计：显示整体进度，不显示成员排名；
- 个人门槛：显示每位成员“已完成／进行中”，可以显示个人进度；
- 共同出勤：显示本周期共同完成天数；
- 达成后保持 100%，不展示超额竞争排名。

## 14. 成就状态

成就仅作为轻量反馈，不单独占用顶层导航。

获得成就时：

- 首页显示一次性卡片；
- 可在共同目标页查看历史成就；统计页不重复展示；
- 不要求用户点击领取；
- 不提供分享、点赞或公开排名；
- 同一成就事件不重复弹出。
- 成就卡片不显示“铜级／银级／金级”文字；星星圆形底色和卡片边框分别使用铜、银、金色；
- 人形图标默认只显示参与人数，鼠标 hover、键盘 focus 或触屏点击后显示成员姓名快照和参与天数；
- 无法可靠还原参与者的旧成就显示“早期成就的参与成员记录暂缺”。

当前成就阶梯：

- 共同相伴：连续 1／3／7 天分别使用铜／银／金色；
- 共同目标：完成 1／3／10 个分别使用铜／银／金色；
- 聚光成炬：累计专注 600／3000／6000 分钟分别使用铜／银／金色。

## 15. 设置页

### 15.1 所有成员可见

- 友间名称；
- 房间时区，只读；
- 当前成员数／上限；
- 当前设备昵称，只读；
- 匿名身份说明；
- PWA 安装入口；
- 应用版本与隐私说明。

MVP 不允许成员自行改昵称，避免历史显示和唯一性产生歧义。

### 15.2 房主功能

#### 友间资料

- 房主可以修改友间名称，名称去除首尾空格后为 1～30 字；
- 房主可以把成员上限提高到 12 人，但不能降低；
- 修改名称或上限不会使当前邀请链接失效；
- 普通成员只能查看名称、当前人数和上限。

#### 邀请

- 显示“复制邀请链接”；
- 显示当前可加入人数；
- “重置邀请链接”为危险操作。

确认文案：

> 重置后，之前发送的邀请链接将立即失效，已加入成员不受影响。

成功后用 Toast：“新邀请链接已生成并复制”。

#### 停用成员

确认框显示：

> 停用后，{昵称} 将无法继续进入这个友间。其历史专注记录仍会保留，且无法转移或删除。

限制：

- 房主不能停用自己；
- 不能停用已停用成员；
- 若成员正在专注，先由服务器按当前时刻结算，再停用；
- 停用不重新计算已经完成的历史共同目标；
- 当前未生效提案的全员范围在停用后重新计算。

## 16. PWA 状态

| 状态 | 页面行为 |
|---|---|
| 可直接安装 | 设置页显示“添加到桌面” |
| 已安装 | 显示“已作为应用使用” |
| iOS Safari | 展示“分享 → 添加到主屏幕”步骤 |
| 不支持安装 | 不显示失效按钮，保留普通网页使用 |
| 新版本可用 | 显示“刷新以使用新版本” |
| 更新失败 | 继续运行旧版本，下次启动重试 |
| 离线启动 | 展示缓存外壳，房间内容标为不可确认 |

Service Worker 不缓存私密房间 API 响应供长期离线浏览，只缓存静态资源和必要应用外壳。

## 17. 权限与身份异常

### 17.1 匿名身份失效

如果浏览器中没有原匿名会话，但用户打开普通首页：

> 当前设备上没有可用身份。匿名身份无法通过昵称恢复。

提供“创建新的友间”和“使用邀请链接重新加入”，不提供按昵称找回。

### 17.2 成员被停用

立即取消 Realtime 订阅并清除本地房间缓存，显示整页状态：

> 你已不能进入这个友间。此前的历史记录仍会保留在友间中。

### 17.3 权限配置异常

如果 API 返回了理论上不应出现的跨房间数据，前端必须丢弃并记录错误；不得为了“继续显示”而绕过服务端权限失败。

## 18. 可访问性与移动端约束

- 所有主要点击目标至少 44×44 CSS 像素；
- 计时变化不通过屏幕阅读器每秒播报；
- 状态变化使用 `aria-live="polite"`，只播报开始、暂停、恢复、结束；
- 颜色不是唯一状态信号；
- 支持系统字体放大到 200%；
- 支持 `prefers-reduced-motion`；
- 键盘可完成开始、暂停、恢复和结束；
- 破坏性按钮不与主操作紧邻，降低误触；
- iOS 安全区内不得放置不可滚动的底部按钮。

## 19. 状态冲突处理规则

### 19.1 重复点击开始

- 第一次请求带幂等键；
- 后续点击被前端拦截；
- 即使产生并发请求，数据库唯一约束也只允许一条活动记录；
- 返回既有记录，不生成第二条。

### 19.2 两个页面同时操作

匿名身份可能在同一设备打开多个标签页：

- 服务端按事务顺序处理；
- 后返回的非法状态转换返回冲突；
- 两个页面均订阅最新服务器结果；
- 用户看到“状态已在另一个页面更新”。

### 19.3 自动结算与手动恢复同时发生

- 服务器先判断暂停是否已满 15 分钟；
- 已到期则结算，拒绝恢复；
- 界面显示自动结算结果；
- 不以客户端按钮点击时间覆盖服务器判断。

### 19.4 自动结算与手动结束同时发生

- 最先在事务中满足的合法转换生效；
- 最终只产生一次完成事件；
- 客户端统一展示服务器返回的结算原因。

## 20. 文案语气规则

- 简洁、事实性，不使用责备语言；
- 不把未打卡描述为失败；
- 不把连接异常描述为作弊；
- 不宣称系统知道用户真实行为；
- 不使用“击败朋友”“落后”等竞争性表达；
- 操作按钮使用动词，例如“点亮台灯”“继续专注”“结束本次”；
- 技术错误提供用户能理解的后果，而非只显示错误码。

推荐错误模板：

```text
发生了什么 + 当前计时是否继续 + 用户现在能做什么
```

示例：

> 暂时无法连接服务器。这次暂停还没有生效，专注计时仍在继续。连接恢复后请重试。

## 21. 前端实现建议

### 21.1 状态分层

不要把所有状态塞进单个页面组件。建议分为：

- `authState`：匿名身份和成员权限；
- `connectionState`：API、Realtime、心跳和同步状态；
- `sessionState`：当前专注状态机；
- `spaceState`：成员与房间配置；
- `statsState`：个人／友间统计；
- `goalState`：提案和目标周期；
- `pwaState`：安装和更新。

### 21.2 服务端状态映射

前端展示状态不应直接等同于数据库字段。例如 `ending` 是前端过渡状态，数据库中仍可能是 `focusing`；只有请求成功后才变为 `completed_notice`。

### 21.3 时间显示

- 每次获取服务端数据时计算客户端与服务器的时间偏移；
- UI 用偏移后的当前时间推算显示；
- 页面重新激活时重新校准；
- 日、周、月统计完全使用服务端聚合结果。

## 22. 状态验收清单

### 创建与加入

- [ ] 创建成功、失败、超时和重复提交均有确定结果；
- [ ] 邀请失效、房间已满、昵称占用和成员停用分别呈现；
- [ ] 身份无法恢复的警告在确认前可见；
- [ ] 加入前不能读取成员任务或统计。

### 专注

- [ ] `idle → starting → focusing` 不产生重复 session；
- [ ] 暂停期间累计时长不增长；
- [ ] 暂停超时后不能恢复；
- [ ] 六小时上限按实际专注时间计算；
- [ ] 不足 5 分钟的记录明确标记为不计入；
- [ ] 所有过渡状态阻止重复点击；
- [ ] 自动与手动操作并发时只结算一次。

### 连接

- [ ] 离线操作不会被伪装成成功；
- [ ] 后台返回先同步再开放按钮；
- [ ] 好友连接不可确认时仍显示正在专注；
- [ ] 不把浏览器离线提示记录成确定事实；
- [ ] 恢复连接后以服务器状态覆盖本地显示。

### 统计与目标

- [ ] 空数据、无效记录、跨日和未确认区间均有展示；
- [ ] 个人与友间时区口径明确；
- [ ] 提案的所有状态均有文案和可执行操作；
- [ ] 目标进度不演变为成员排行榜；
- [ ] 达成和未达成都保留历史结果。

### 权限与 PWA

- [ ] 房主与成员操作入口不同；
- [ ] 停用成员立即失去访问权限；
- [ ] PWA 可安装、已安装、不支持和更新状态均有处理；
- [ ] 私密 API 响应不被 Service Worker 长期缓存。

## 23. 已冻结产品决策

以下产品决策已经确认，页面、统计和服务端实现必须遵守。

### D1. 每日打卡门槛

每天累计 **1 小时（60 分钟）**完成个人打卡。

影响范围：今日摘要、连续打卡、共同出勤目标、成就和首版成功指标。

### D2. 一个身份能否加入多个友间

MVP 正式规则：**一个匿名身份只能加入一个友间**。

理由：允许多个友间会引入房间切换、专注记录归属、跨房间同时可见性和多房间通知问题，偏离两三位固定好友的核心场景。数据库关系可以预留未来扩展，但首版界面不开放。

影响范围：欢迎页、加入第二个邀请链接时的行为、导航和统计归属。

首版按“一个身份一个友间”制作线框图和实现；数据库不使用会永久阻止未来多友间扩展的结构。
